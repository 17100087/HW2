%h1 Delete a movie

= form_tag movies_deletenew_path :method => :post do

  = label :movie, :title, 'Title'
  = text_field :movie, 'title'

  = label :movie, :rating, 'Rating'
  = select :movie, :rating, ['G','PG','PG-13','R','NC-17']

  

  = submit_tag 'Delete Movie'
